# DV_Group-Activity

Dashboard phân tích dữ liệu Superstore với Streamlit.


## Cài đặt

Cài đặt dependencies:
```bash
pip install -r requirements.txt
```

## Chạy ứng dụng

```bash
streamlit run app.py
```

Ứng dụng sẽ mở tại `http://localhost:8501`
